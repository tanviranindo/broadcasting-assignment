import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.assignment.app',
  appName: 'Broadcast Assignment',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
