import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.assignment.app",
  appName: "Broadcasting Assignment",
  webDir: "dist/spa",
  bundledWebRuntime: false,
};

export default config;
