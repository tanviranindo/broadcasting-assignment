<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.assignment.broadcastingassignment">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Broadcastingassignment">
        <activity
            android:name=".ToasterBroadcasting"
            android:exported="false"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".TextBroadcastReceiver"
            android:exported="false" />
        <activity
            android:name=".SystemBroadcastSender"
            android:exported="false" />
        <activity
            android:name=".WIFIBroadcastSender"
            android:exported="false" />
        <activity
            android:name=".TextBroadcastSender"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".ToasterBroadcasting" />
    </application>

</manifest>